nextflow_pipeline {

    name "Test pipeline"
    script "../main.nf"
    profile "test,docker"

    test("Pipeline runs with test profile") {
        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assert workflow.success
        }
    }
}
