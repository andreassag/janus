{
  "name": "janus",
  "version": "1.0.0-dev",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:editorconfig": "editorconfig-checker",
    "lint": "npm run format:check && npm run lint:editorconfig"
  },
  "devDependencies": {
    "editorconfig-checker": "^6.0.1",
    "prettier": "3.9.6",
    "prettier-plugin-groovy": "^0.2.1"
  }
}
